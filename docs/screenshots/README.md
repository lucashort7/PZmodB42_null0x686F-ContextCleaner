# Screenshots

Pending capture. Expected files (referenced by `../workshop_description.txt`):

- `contextmenu-before.png` — a cluttered right-click context menu (inventory or world), no filtering applied.
- `contextmenu-after.png` — the same menu with ContextCleaner rules applied.
- `contextcleanerwindow-ui.png` — the `ContextCleanerWindow` config UI (rule registry: Pattern / Type / Action / Scope).

Once added, upload each to an image host reachable from Steam (e.g. GitHub raw URL, imgur) and swap the `[img]...[/img]` placeholders in `workshop_description.txt` with the real URLs.
